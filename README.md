# cp_cnn_transformers
CNNs and Transformers from scratch in PyTorch — custom CNN, ResNet-18 with residual blocks, and a full Transformer (attention, encoder/decoder, positional encoding) trained on MiniImageNet and TinyStories.
